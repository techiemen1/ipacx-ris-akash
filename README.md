# Test Project

Standard test deployment workspace for node application testing and containerized setup.

## Quick Start

```bash
# Build and test containers locally
./setup_docker.sh

# Deploy via Docker Compose
docker compose up -d
```